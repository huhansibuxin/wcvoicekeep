feat(wcvoicekeep): 纯 daemon 检测 PiP 失效自动重建——suspend_count 判挂起 + 音频门控 + 保留微信拉起 (v1.9.13)

背景（老板实机铁证）：
- 打完微信视频：wetype 进程 Mach Task State=S▲B（系统挂起）、CPU 0%、内存仅 4.8MB
- Apple 文档的 isPictureInPictureSuspended 自动恢复不生效（进程被挂起跑不动）
- 必须 daemon 重拉。方案 B 可行性已实测确认：
  v1.9.12 加 task_for_pid-allow 后，root daemon -> mobile wetype task_for_pid 0 OK、
  suspend_count 可读（wetype 带 PiP 活着时 =0，被顶挂起后应 >0）

实现（全部挂在既有 60s 守护 tick，无任何新定时器/心跳/磁盘写——老板要求去轮询）：
- procPid("wxkb") 精确拿 wetype 主进程 pid（v1.9.4 strcmp 排除 wxkb_plugin）
- taskSuspendCount(pid)：task_for_pid + task_info 读 suspend_count
- 主循环 gPipUp 分支：suspend_count>0 = 进程被挂起 = PiP 被顶掉 -> handlePiPLost()
  - 锁屏 -> 等解锁（lockstate 回调兜底）
  - 其他 App 在播音频（视频 PiP/音乐，AudioSessionGetProperty 'othr' 手写声明防
    -Wdeprecated-declarations 被 -Werror 判死）-> 不拉，绝不打断看视频
  - 5min 冷却（失败不反复闪）；空闲解锁态 -> reWarm 前台拉起重建 PiP
- 微信拉起保留（watchWechat 被杀就无头拉 + 开机 30s 错开拉 + 120s 节流）——老板要求
  微信也要能拉起来；wetype 死了/僵尸分支原样保留

改动：daemon.m（procPid/taskSuspendCount/otherAudioPlaying/handlePiPLost + 主循环）、
Makefile 加 AudioToolbox、entitlements 已在 v1.9.12 加 task_for_pid-allow、
control 1.9.12 -> 1.9.13。

验证：装 v1.9.13 后打微信视频 PiP -> 挂断 -> daemon 日志应出现
  "SUSPENDED (suspend_count=N)" -> "PiP lost ... re-warm" -> wetype 重新闪屏建 PiP
