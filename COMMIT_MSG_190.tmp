feat(wcvoicekeep): 架构改为「开机前台预热一次 + PiP 自保活」，删音频保活 (v1.9.0)

背景（老板拍板 + 硬限制）：
  - 后台起 PiP 被系统硬拒：AVKit 错误 -1001 "UIScene activation state other than
    UISceneActivationStateForegroundActive" —— 无任何私有 API 可绕（搜索/实测双重确认）。
  - v1.8.8 音频保活能保活但 media 服务常驻耗电（老板否决）；且保活也解决不了点语音跳转
    （PiP 没建起来）。
  - 唯一可行 = 开机前台预热一次建 PiP，之后 PiP 自保活（0 CPU、0 跳转）。

daemon.m：
  - launchBackground（无头后台拉起）→ warmupForeground（前台预热）：
      (1) 首选 SBSLaunchApplicationForDebugging 带 --wcvk-warmup 参数（dylib 据此自动退后台）
      (2) 兜底 SBSLaunchApplicationWithIdentifier(flag=0) 前台激活（dylib 用启动<8s 启发式）
      (3) 最后兜底 LSA openApplicationWithBundleID:active:YES
  - checkAndLaunch 节流：成功拉起后 15min 内不重复预热（防反复闪屏）；
    锁屏期失败不记录 gLastWarmup -> 每 60s 重试（锁屏下无闪屏可见）。

Tweak.xm：
  - 删 StartSilentKeepAlive（音频保活，0 CPU 目标）
  - 新增预热判定 IsWarmup()（--wcvk-warmup 参数 || 启动后<8s）+ AutoBackground()
    （[[UIApplication suspend]] 模拟按 Home；已在后台则跳过）
  - EnsureStandbyPiP：PiP 建好(isActive=1)且预热 -> 立即自动退后台；
    安全网 t>=4s 仍无 PiP 且预热 -> 照样退后台（绝不让 UI 占屏）
  - TriggerOnMain：删音频保活调用；后台 trigger 跳过 PiP（预热负责建）
  - CTOR 记录 gLaunchTime + 扫描 --wcvk-warmup 参数

Makefile 去 AVFAudio；control 1.8.8 -> 1.9.0。

预期效果：
  - 注销/重启 -> 设备解锁后 daemon 前台拉起 wetype（闪 ~0.5s）
  - dylib 建 PiP（isActive=1）-> 自动退后台
  - PiP 常驻 = 自保活（无 media 服务、0 CPU）；点语音按钮不跳转

老板实测关注（装 v1.9.0 deb + TrollFools 重注入 WCVoiceKeep-dylib）：
  - wcvoicekeep.log: WARMUP arg=1 + PROBE t=0.8s isActive=1 + WARMUP ... [UIApplication suspend]
  - ps aux | grep wxkb: 状态非 ?<s，长存活
  - 点语音按钮：不跳主 App（PiP 直接接住）
  - daemon err: 只有首次 "foreground warmup"，之后稳定 "alive -> only trigger dylib"
