fix(wcvoicekeep): daemon 改用 SBSLaunchApplicationWithIdentifier(bid, Background=1) 真无头后台启动，根治弹 UI (v1.8.7)

根因：LSApplicationWorkspace openApplicationWithBundleID:active:NO 在 iOS 16 实测仍弹 UI——active:NO 只表示不抢已运行 App 前台，新 App launch 时界面照常渲染，无法做到无头。

真无头（同支付宝静默推送唤醒主 App 进后台不显 UI）必须走 SpringBoardServices 私有 API：
  int SBSLaunchApplicationWithIdentifier(CFStringRef bid, int flags);
  flags = 1 (SBSApplicationLaunchFlagBackground，sbutils -b 源码佐证 flags|=1)
  -> 主 App 收到 didFinishLaunching 即进 background，绝不上前台、不渲染 UI。

改动(daemon.m)：
  - 新增 sbsReady()：dlopen SpringBoardServices + dlsym SBSLaunchApplicationWithIdentifier，
    顺带调 SBSSpringBoardServerPort 建端口；iOS16 该 framework 二进制合进 dyld cache，dlopen 可能失败。
  - launchBackground() 首选 SBS background 启动(flag=1)，r==0 即成功且无 UI；
    仅当 SBS 不可用(dlopen/dlsym 失败)才回退 LSA active:NO（兜底，会带 UI）。
  - LOG 详细记录 SBS 是否 ready / 返回值，便于老板实测判定走哪条路径。
  - 顶部注释纠正 v1.8.6 误判（active=NO 非真无头）。

entitlements.plist 的 com.apple.springboard.launchapplications 已是 SBS 所需权限，无需改。
control 1.8.6 -> 1.8.7。

老板实测关注：装后看 /var/log/wcvoicekeep.daemon.err 是否出现
  SBS ready ...
  SBSLaunchApplicationWithIdentifier(com.tencent.wetype, Background=1) -> 0 (0=ok)
若 SBS dlopen 失败(Fallback to LSA)，说明 iOS16 该 framework 不可 dlopen，走后续 FBSSystemService 方案。
