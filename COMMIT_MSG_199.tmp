fix(wcvoicekeep): 回退息屏门控，改回"死了就拉"，保证注销后第一次必拉 (v1.9.9)

老板判断正确：v1.9.7/1.9.8 注销后拉不起来的根因 = 息屏门控。
链条：注销后锁屏（典型）-> 开机 2min 预热无法激活完不成 -> 解锁后屏幕亮 ->
门控"亮屏不拉"拒绝重拉 -> 有密码时即使息屏重拉也只是僵尸，解锁又 hold -> 永远拉不起来。

v1.9.9 回退（老板拍板：死了就拉，接受偶尔亮屏闪一下换可靠拉起）：
  1. 看护循环删掉 gScreenBlank 门控：wetype 死了/僵尸 -> 无条件 kill+重拉。
  2. 删 gBootPhase（不再需要）。
  3. hasBlankedScreen 回调：屏幕状态一变（息屏 OR 亮屏/解锁）只要 gPipUp==NO 就立即
     后台 reWarm()——解锁瞬间补完"注销后锁屏期没完成的预热"，保证第一次必拉。
  4. reWarm 的 gWarmingUp 防并发保留；微信看护/开机 30s 拉微信保留。

三条独立路径（开机 2min 循环 / 60s 看护 / 屏幕事件即时重拉）都不受屏幕状态限制，
注销后第一次拉起有保障。

control 1.9.8 -> 1.9.9。仅 daemon.m，装 deb 即可。
