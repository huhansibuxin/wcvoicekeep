fix(wcvoicekeep): 开机重试到成功 + 中途死亡仅息屏重拉 + 修 -Werror 未用常量 (v1.9.3)

老板确认方案（不搞 SB 注入，有锁屏密码）：
  1. 注销后立即预热：开机循环一直重试到 pip.built（有密码则等你解锁激活，锁屏期无感），
     不再用有界 reWarm 导致"进程起了但没 PiP"的僵尸卡死。
  2. 中途死了/被杀：只在息屏时重拉（gScreenBlank 门控）；亮屏绝不重拉——
     天然满足"有视频 PiP 时不打扰"（iPhone 看 PiP 屏幕必亮；息屏 PiP 必停）。
  3. 息屏重拉用 UnlockDevice(4) 亮屏唤醒（v1.9.2 已实现）；有锁屏密码则激活等解锁。
  4. 修编译失败：daemon.m 有 -Werror，v1.9.2 遗留未使用常量 kFastRetrySec/kWarmupMinInterval
     直接报错。kFastRetrySec 由开机循环用回；kWarmupMinInterval 删除。

daemon.m：
  - main(): 开机 while(!gPipUp){ warmupOnce(); sleep(kFastRetrySec); } 无限重试到成功
  - 守护循环：活着 trigger；死了 && 息屏 -> reWarm()；死了 && 亮屏 -> 跳过（降级）
  - 删 kWarmupMinInterval

control 1.9.2 -> 1.9.3。

老板实测：
  - 注销 -> 解锁后立即闪一次建 PiP（不再等）
  - 亮屏时杀 wetype -> 不闪不打扰；息屏后自动静默重拉恢复
  - 有锁屏密码：息屏重拉会亮屏但激活在你解锁瞬间完成
  - 重注入 WCVoiceKeep-dylib！
