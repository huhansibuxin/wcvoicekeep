fix(wcvoicekeep): 预热时机提前 + 闪屏缩短 + 前台绝不中途闪 (v1.9.1)

老板反馈三个问题，逐一修：
  1. 闪屏时间长：dylib 首次 isActive 检测 0.8s -> 0.4s，建完 PiP 立即退后台
  2. 注销后 ~1min 才闪：原 60s 轮询 + isAppRunning 无法区分「带 PiP 活着」vs「锁屏僵尸」。
     改为：dylib 建好 PiP 后反向发 Darwin 通知 com.wcvoicekeep.pip.built，
     daemon 收到才算预热成功；开机 3s 后进入快循环（每 ~20s 重试，锁屏期无感），
     解锁后 ~5s 内闪屏建 PiP。锁屏期进程起了但没 PiP -> SBSProcessIDForDisplayIdentifier
     拿 pid + SIGKILL 重试激活。
  3. 前台用着 App 会不会闪：新增 hasBlankedScreen 屏幕状态监听；
     屏幕亮时 App 死了 -> 绝不自动重预热（降级到下次注销，杜绝中途切换闪屏）；
     屏幕熄灭时 -> 静默重预热（带 15min 节流）。

daemon.m：
  - 配置：kBootDelaySec 10->3；新增 kFastRetrySec=5 / kPipWaitTimeoutSec=15 / kKillStaleSec=25
  - registerNotifs()：pip.built（后台队列，main 线程 sleep 轮询时也能置位）+ hasBlankedScreen
  - killApp()：SBSProcessIDForDisplayIdentifier + SIGKILL
  - warmupOnce()：快循环单次尝试，等 pip.built 最多 15s
  - main()：快预热循环 -> 60s 看护（亮屏不重预热，熄屏静默重预热）

Tweak.xm：
  - WarmupDone()：notify_post(com.wcvoicekeep.pip.built) + AutoBackground
  - 检测节奏 [0.4,1.0,1.8] + 尾部 [2.5,4,8]，attempt<3

control 1.9.0 -> 1.9.1。

老板实测：
  - 注销 -> 解锁后 ~5s 内闪一次（不再等 1min）
  - wcvoicekeep.log: PROBE t=0.4s isActive=1 -> WARMUP posted pip.built -> suspend
  - daemon err: 首次 "foreground warmup" 后 "PIP.BUILT received -> warmup success"，
    之后 60s 稳定 alive；前台用其他 App 时 wetype 死了不会闪
  - 重注入 WCVoiceKeep-dylib！
