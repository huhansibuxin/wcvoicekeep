fix(wcvoicekeep): dylib 静音音频保活，根治无头拉起后不保活 (v1.8.8)

全链路实测（daemon 日志 + wetype dylib 沙盒日志交叉验证）：
  - daemon 无头拉起 ✅：SBSLaunchApplicationWithIdentifier(bid, Background=1) -> 0，1s 内 wetype 起来
  - dylib 注入 ✅：CTOR injected，WCVoiceKeep.dylib 在 wxkb.app/Frameworks
  - PiP 启动 ❌：后台拉起后调 startWithCompletionHandler: 全程 isActive=0（t=0.8/1.6/2/4/8s 都 0）；
    15:58:42 App 前台激活瞬间 isActive=1 -> 证明 startPictureInPicture 平台硬限制必须激活态
  - 保活 ❌：无 PiP -> 无后台保活凭证 -> 进程 ?<s（挂起）-> 被杀 -> daemon 重拉死循环

根因：wetype Info.plist 有 UIBackgroundModes=['audio']，但后台音频模式只有真正播放/录音才生效；
之前 dylib 没播任何音频，App 后台即被挂起。

修复（Tweak.xm）：
  1. 新增 StartSilentKeepAlive()：AVAudioSession Playback + MixWithOthers（不打断用户音乐），
     生成 1s 静音 WAV 无限循环播放（volume=0.0 即可，后台资格看会话不看音量）-> App 永不挂起。
  2. TriggerOnMain 保活优先：先 StartSilentKeepAlive()；再判 applicationState，
     非 Active 只保活不建 PiP（后台建 PiP 必然失败，跳过避免无效重试刷日志），
     前台激活时照常 EnsureStandbyPiP。
  3. daemon 每 60s trigger 会反复 re-arm 保活，即使被 wetype 自己打断也能恢复。

Makefile: WCVoiceKeep_FRAMEWORKS 加 AVFAudio。control 1.8.7 -> 1.8.8。

验证：装 v1.8.8 后
  - ps aux | grep wxkb -> 状态不再 ?<s（挂起），长时间保持存活（daemon 日志不再每轮 NOT running 重拉）
  - wcvoicekeep.log 出现 KEEPALIVE silent audio loop STARTED
  - App 前台激活时 PiP 照常建（isActive=1），且因 audio 模式后台持续
